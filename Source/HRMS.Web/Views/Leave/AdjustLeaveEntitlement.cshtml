@{
    Layout = "_Layout";
    ViewData["Title"] = "Adjust Leave Entitlement";
}

<div class="card">
    <div class="card-header">
        <h2 class="card-title">Adjust Leave Entitlement</h2>
    </div>

    @if (TempData["Success"] != null)
    {
        <div class="alert alert-success">@TempData["Success"]</div>
    }

    <form method="post" asp-action="AdjustLeaveEntitlement">
        <div class="form-group">
            <label for="employeeId">Employee *</label>
            <select id="employeeId" name="employeeId" required class="form-control">
                <option value="">-- Select Employee --</option>
                @if (ViewBag.Employees != null)
                {
                    @foreach (var emp in (List<Employee>)ViewBag.Employees)
                    {
                        <option value="@emp.EmployeeId">@emp.FullName (@emp.Email)</option>
                    }
                }
            </select>
        </div>

        <div class="form-group">
            <label for="leaveTypeId">Leave Type *</label>
            <select id="leaveTypeId" name="leaveTypeId" required class="form-control">
                <option value="">-- Select Leave Type --</option>
                @if (ViewBag.LeaveTypes != null)
                {
                    @foreach (var leaveType in (List<Leave>)ViewBag.LeaveTypes)
                    {
                        <option value="@leaveType.LeaveId">@leaveType.LeaveType</option>
                    }
                }
            </select>
        </div>

        <div class="form-group">
            <label for="adjustment">Adjustment (Days) *</label>
            <input type="number" id="adjustment" name="adjustment" step="0.5" required class="form-control" placeholder="e.g., +5 or -2" />
            <small class="form-text text-muted">Use positive numbers to add days, negative numbers to subtract days</small>
        </div>

        <button type="submit" class="btn btn-primary" style="width: 100%;">Adjust Entitlement</button>
    </form>

    <div style="margin-top: 20px;">
        <a asp-action="ManageLeaveTypes">Back to Manage Leave Types</a>
    </div>
</div>

